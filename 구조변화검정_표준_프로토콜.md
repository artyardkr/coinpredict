# 구조변화 검정 표준 프로토콜 (Structural Change Testing Protocol)

**Bitcoin ETF Impact Analysis - Standardized Testing Framework**

**작성일**: 2025-11-09
**버전**: 1.0
**목적**: 재현 가능하고 체계적인 구조변화 검정 프로세스 정립

---

## 📋 목차

1. [개요](#1-개요)
2. [3단계 검정 프로토콜](#2-3단계-검정-프로토콜)
3. [데이터 준비 (표준화)](#3-데이터-준비-표준화)
4. [검정 1: Chow Test](#4-검정-1-chow-test)
5. [검정 2: Quandt-Andrews Test](#5-검정-2-quandt-andrews-test)
6. [검정 3: CUSUM Test](#6-검정-3-cusum-test)
7. [결과 해석 기준](#7-결과-해석-기준)
8. [표준 보고 양식](#8-표준-보고-양식)
9. [체크리스트](#9-체크리스트)

---

## 1. 개요

### 1.1 목적

구조변화 검정의 **재현 가능성**, **일관성**, **신뢰성**을 보장하기 위한 표준화된 프로토콜

### 1.2 적용 대상

- 시계열 데이터의 구조변화 검정
- 특정 이벤트(예: ETF 승인)의 영향 분석
- 여러 변수의 일관된 검정

### 1.3 핵심 원칙

1. **표준화**: 모든 변수에 동일한 기준 적용
2. **다각도 검증**: 3가지 독립적 검정으로 교차 확인
3. **통계적 엄격성**: 다중 검정 보정, HAC 표준오차 적용
4. **재현 가능성**: 모든 단계 문서화 및 코드 공개

---

## 2. 3단계 검정 프로토콜

### 2.1 검정 개요

```
┌─────────────────────────────────────────────────────────┐
│                  구조변화 검정 파이프라인                 │
└─────────────────────────────────────────────────────────┘

단계 0: 데이터 표준화
   ↓
┌──────────────────┐
│ Step 1           │
│ Chow Test        │  목적: 특정 날짜(ETF 승인일)에 구조변화 확인
│ (Known Break)    │  입력: 변화점 날짜 지정
└──────────────────┘  출력: F-통계량, p-value
   ↓
┌──────────────────┐
│ Step 2           │
│ Quandt-Andrews   │  목적: 데이터에서 변화점 찾기
│ (Unknown Break)  │  입력: 탐색 구간 (15%-85%)
└──────────────────┘  출력: sup F-통계량, 변화점 날짜
   ↓
┌──────────────────┐
│ Step 3           │
│ CUSUM Test       │  목적: 점진적 vs 급격한 변화 구분
│ (Change Type)    │  입력: 누적합 경계선
└──────────────────┘  출력: 경계 이탈 여부, 변화 속도
   ↓
결과 통합 및 해석
```

### 2.2 검정 간 관계

| 검정 | 변화점 | 목적 | 상호보완 |
|------|--------|------|---------|
| **Chow** | 알려진 날짜 | "ETF 날짜에 정말 변했나?" | QA와 날짜 비교 |
| **Quandt-Andrews** | 데이터에서 탐색 | "진짜 변화점이 언제?" | Chow 결과 검증 |
| **CUSUM** | 변화 유형 | "어떻게 변했나?" | 변화 속도 파악 |

---

## 3. 데이터 준비 (표준화)

### 3.1 필수 전처리

#### Step 0.1: 결측치 처리

```python
# 표준 방법: Forward Fill → Backward Fill → Median
df = df.fillna(method='ffill').fillna(method='bfill').fillna(df.median())

# 검증
assert df.isnull().sum().sum() == 0, "결측치가 남아있음!"
```

#### Step 0.2: 이상치 처리 (Winsorization)

```python
from scipy.stats.mstats import winsorize

# 상하위 1% 절사 (표준)
for col in df.select_dtypes(include=[np.number]).columns:
    df[col] = winsorize(df[col], limits=[0.01, 0.01])
```

**이유**: 비트코인의 극심한 변동성(일일 ±30%) 영향 최소화

#### Step 0.3: 변수 표준화 (Z-Score)

```python
from sklearn.preprocessing import StandardScaler

# 각 변수를 평균 0, 표준편차 1로 표준화
scaler = StandardScaler()
df_scaled = pd.DataFrame(
    scaler.fit_transform(df),
    columns=df.columns,
    index=df.index
)
```

**장점**:
- ✅ 변수 간 스케일 차이 제거 (DFF: 0-5 vs bc_hash_rate: 수억)
- ✅ F-통계량 비교 가능
- ✅ 계수 해석 일관성

#### Step 0.4: 변화점 날짜 설정

```python
# 표준 설정
BREAKPOINT_DATE = '2024-01-10'  # ETF 승인일
TRIM_PERCENT = 0.15  # QA Test 탐색 구간 (15%-85%)
```

---

## 4. 검정 1: Chow Test

### 4.1 목적

**"ETF 승인일(2024-01-10)을 기준으로 회귀계수가 유의미하게 변했는가?"**

### 4.2 가설

```
H₀ (귀무가설): β₁ = β₂ (ETF 전후 계수 동일, 구조변화 없음)
H₁ (대립가설): β₁ ≠ β₂ (ETF 전후 계수 다름, 구조변화 있음)
```

### 4.3 표준 프로세스

#### Step 1.1: 데이터 분할

```python
# 표준 분할점
breakpoint_date = pd.to_datetime('2024-01-10')
breakpoint_idx = df.index.get_loc(breakpoint_date)

# 분할
df_pre = df.iloc[:breakpoint_idx]    # ETF 이전
df_post = df.iloc[breakpoint_idx:]   # ETF 이후

print(f"ETF 이전: {len(df_pre)}일 ({df_pre.index[0]} ~ {df_pre.index[-1]})")
print(f"ETF 이후: {len(df_post)}일 ({df_post.index[0]} ~ {df_post.index[-1]})")
```

#### Step 1.2: 단변량 회귀 (핵심!)

```python
import statsmodels.api as sm

def chow_test_standardized(y, X, breakpoint_idx):
    """
    표준화된 Chow Test

    Parameters:
    -----------
    y : pd.Series (표준화된 종속변수)
    X : pd.DataFrame (표준화된 독립변수, constant 포함)
    breakpoint_idx : int (변화점 인덱스)

    Returns:
    --------
    dict: F-통계량, p-value, 계수 변화
    """

    # 1. 전체 회귀
    model_full = sm.OLS(y, X).fit(cov_type='HAC', cov_kwds={'maxlags': 5})
    rss_full = model_full.ssr

    # 2. 분할 회귀
    X1, y1 = X[:breakpoint_idx], y[:breakpoint_idx]
    X2, y2 = X[breakpoint_idx:], y[breakpoint_idx:]

    model1 = sm.OLS(y1, X1).fit(cov_type='HAC', cov_kwds={'maxlags': 5})
    model2 = sm.OLS(y2, X2).fit(cov_type='HAC', cov_kwds={'maxlags': 5})

    rss1 = model1.ssr
    rss2 = model2.ssr

    # 3. Chow F-통계량
    n1, n2 = len(y1), len(y2)
    k = X.shape[1]  # 변수 개수

    numerator = (rss_full - (rss1 + rss2)) / k
    denominator = (rss1 + rss2) / (n1 + n2 - 2*k)

    F_stat = numerator / denominator
    p_value = 1 - f.cdf(F_stat, k, n1 + n2 - 2*k)

    return {
        'F_stat': F_stat,
        'p_value': p_value,
        'coef_pre': model1.params[1],   # constant 제외
        'coef_post': model2.params[1],
        'coef_change': model2.params[1] - model1.params[1],
        'coef_change_pct': (model2.params[1] / model1.params[1] - 1) * 100
    }
```

#### Step 1.3: 다중 검정 보정

```python
from statsmodels.stats.multitest import multipletests

# Bonferroni 보정 (보수적)
alpha = 0.05
n_tests = len(variables)
bonferroni_alpha = alpha / n_tests

# FDR 보정 (Benjamini-Hochberg)
reject_fdr, pvals_corrected_fdr, _, _ = multipletests(
    p_values,
    alpha=alpha,
    method='fdr_bh'
)

# 결과
print(f"Bonferroni α: {bonferroni_alpha:.6f} (유의 시 매우 확실)")
print(f"FDR 유의 변수: {reject_fdr.sum()} / {n_tests}")
```

### 4.4 표준 출력

```python
# 표준 DataFrame 형식
chow_results = pd.DataFrame({
    'Variable': variables,
    'F_stat': f_stats,
    'p_value': p_values,
    'p_value_bonferroni': p_values_bonferroni,
    'p_value_fdr': p_values_fdr,
    'Significant_Bonferroni': reject_bonf,
    'Significant_FDR': reject_fdr,
    'Coef_Pre': coefs_pre,
    'Coef_Post': coefs_post,
    'Coef_Change': coef_changes,
    'Coef_Change_Pct': coef_change_pcts
})

# 정렬: F-통계량 내림차순
chow_results = chow_results.sort_values('F_stat', ascending=False)

# CSV 저장 (표준 경로)
chow_results.to_csv('results/chow_test_results.csv', index=False)
```

---

## 5. 검정 2: Quandt-Andrews Test

### 5.1 목적

**"데이터에서 발견되는 진짜 변화점은 언제인가?"**

### 5.2 가설

```
H₀: 모든 τ에서 구조변화 없음
H₁: 어떤 τ에서 구조변화 있음

τ: 가능한 모든 변화점 (15% ~ 85% 구간)
```

### 5.3 표준 프로세스

#### Step 2.1: 탐색 구간 설정

```python
# 표준 설정: 15% ~ 85% (Andrews 1993 권장)
n = len(df)
start_idx = int(n * 0.15)
end_idx = int(n * 0.85)

print(f"탐색 구간: {df.index[start_idx]} ~ {df.index[end_idx]}")
print(f"탐색 날짜 수: {end_idx - start_idx}일")
```

#### Step 2.2: sup F-통계량 계산

```python
def quandt_andrews_test_standardized(y, X, trim=0.15):
    """
    표준화된 Quandt-Andrews Test

    Parameters:
    -----------
    y : pd.Series (표준화된 종속변수)
    X : pd.DataFrame (표준화된 독립변수)
    trim : float (탐색 제외 비율, 양쪽)

    Returns:
    --------
    dict: sup F-통계량, 변화점, 모든 F-통계량
    """

    n = len(y)
    start = int(n * trim)
    end = int(n * (1 - trim))

    f_stats = []
    breakpoints = []

    # 모든 가능한 변화점에서 Chow Test
    for tau in range(start, end):
        result = chow_test_standardized(y, X, tau)
        f_stats.append(result['F_stat'])
        breakpoints.append(y.index[tau])

    # sup F-통계량 (최대값)
    sup_f = max(f_stats)
    sup_f_idx = f_stats.index(sup_f)
    sup_f_date = breakpoints[sup_f_idx]

    return {
        'sup_F': sup_f,
        'breakpoint_date': sup_f_date,
        'breakpoint_idx': start + sup_f_idx,
        'all_F_stats': f_stats,
        'all_breakpoints': breakpoints
    }
```

#### Step 2.3: Critical Values (Andrews 1993)

```python
# Andrews (1993) Critical Values (trim=0.15)
ANDREWS_CRITICAL_VALUES = {
    0.10: 7.78,   # 10% 유의수준
    0.05: 9.21,   # 5% 유의수준
    0.01: 12.16   # 1% 유의수준
}

def interpret_qa_test(sup_f, alpha=0.05):
    """sup F-통계량 해석"""
    critical_value = ANDREWS_CRITICAL_VALUES[alpha]

    if sup_f > critical_value:
        return f"유의 (sup F = {sup_f:.2f} > {critical_value})"
    else:
        return f"비유의 (sup F = {sup_f:.2f} ≤ {critical_value})"
```

### 5.4 표준 출력

```python
# 표준 DataFrame
qa_results = pd.DataFrame({
    'Variable': variables,
    'sup_F': sup_f_stats,
    'Breakpoint_Date': breakpoint_dates,
    'Significant_5pct': [f > 9.21 for f in sup_f_stats],
    'Significant_1pct': [f > 12.16 for f in sup_f_stats]
})

# 정렬
qa_results = qa_results.sort_values('sup_F', ascending=False)

# CSV 저장
qa_results.to_csv('results/quandt_andrews_results.csv', index=False)
```

---

## 6. 검정 3: CUSUM Test

### 6.1 목적

**"구조변화가 점진적인가, 급격한가?"**

### 6.2 가설

```
H₀: 안정적 (CUSUM이 경계 내)
H₁: 불안정 (CUSUM이 경계 이탈)

경계 이탈 = 급격한 변화 (1일 만에 뒤바뀜)
경계 내 = 점진적 변화 (몇 주~몇 달)
```

### 6.3 표준 프로세스

#### Step 3.1: CUSUM 계산

```python
def cusum_test_standardized(y, X):
    """
    표준화된 CUSUM Test

    Parameters:
    -----------
    y : pd.Series (표준화된 종속변수)
    X : pd.DataFrame (표준화된 독립변수)

    Returns:
    --------
    dict: CUSUM 경로, 경계선, 이탈 여부
    """

    # 1. 전체 회귀로 잔차 계산
    model = sm.OLS(y, X).fit()
    residuals = model.resid

    # 2. 표준화된 잔차
    std_residuals = residuals / residuals.std()

    # 3. 누적합
    cusum = std_residuals.cumsum()

    # 4. 경계선 (Brown-Durbin-Evans, 1975)
    n = len(y)
    boundary = 0.948 * np.sqrt(n)  # 5% 유의수준

    # 5. 경계 이탈 확인
    breaches = (cusum.abs() > boundary).sum()

    return {
        'cusum': cusum,
        'boundary': boundary,
        'n_breaches': breaches,
        'max_cusum': cusum.abs().max(),
        'breach_ratio': cusum.abs().max() / boundary
    }
```

#### Step 3.2: 해석 기준

```python
def interpret_cusum(result):
    """CUSUM 결과 해석"""

    breach_ratio = result['breach_ratio']

    if result['n_breaches'] > 0:
        return {
            'type': 'ABRUPT',
            'description': '급격한 변화 (하루 만에 뒤바뀜)',
            'speed': 'VERY_FAST',
            'interpretation': '경계 이탈 발생 → 단절적 구조변화'
        }
    elif breach_ratio > 0.8:
        return {
            'type': 'RAPID',
            'description': '빠른 점진적 변화 (1-2주)',
            'speed': 'FAST',
            'interpretation': '경계 근접 → 빠른 변화'
        }
    elif breach_ratio > 0.5:
        return {
            'type': 'MODERATE',
            'description': '중간 속도 점진적 변화 (1-2개월)',
            'speed': 'MODERATE',
            'interpretation': '경계 내 → 점진적 변화'
        }
    else:
        return {
            'type': 'GRADUAL',
            'description': '느린 점진적 변화 (3-6개월)',
            'speed': 'SLOW',
            'interpretation': '경계 내 → 매우 점진적 변화'
        }
```

### 6.4 표준 출력

```python
# 표준 DataFrame
cusum_results = pd.DataFrame({
    'Variable': variables,
    'Max_CUSUM': max_cusums,
    'Boundary': boundaries,
    'Breach_Ratio': breach_ratios,
    'N_Breaches': n_breaches,
    'Change_Type': change_types,
    'Change_Speed': change_speeds
})

# CSV 저장
cusum_results.to_csv('results/cusum_test_results.csv', index=False)
```

---

## 7. 결과 해석 기준

### 7.1 3단계 통합 의사결정 트리

```
┌─────────────────────────────────────┐
│ Chow Test 결과                       │
└─────────────────────────────────────┘
         │
         ├─ p < 0.00042 (Bonferroni) ─────┐
         │                                  │
         └─ p ≥ 0.00042 ──────────────────┤
                                           │
┌──────────────────────────────────────┐  │
│ Quandt-Andrews Test 결과             │  │
└──────────────────────────────────────┘  │
         │                                 │
         ├─ sup F > 9.21 ─────────────────┤
         │                                 │
         └─ sup F ≤ 9.21 ─────────────────┤
                                           │
┌──────────────────────────────────────┐  │
│ CUSUM Test 결과                      │  │
└──────────────────────────────────────┘  │
         │                                 │
         ├─ 경계 이탈 ──────► 급격한 변화  │
         │                                 │
         └─ 경계 내 ────────► 점진적 변화  │
                                           │
                                           ▼
                                    최종 결론
```

### 7.2 표준 해석표

| Chow | QA | CUSUM | 결론 | 신뢰도 |
|------|-------|-------|------|--------|
| ✅ 유의 | ✅ 유의 | 🔴 이탈 | **급격하고 확실한 구조변화** | ★★★★★ |
| ✅ 유의 | ✅ 유의 | ✅ 내 | **점진적이지만 확실한 구조변화** | ★★★★☆ |
| ✅ 유의 | ❌ 비유의 | ✅ 내 | **ETF 날짜는 맞지만 점진적** | ★★★☆☆ |
| ❌ 비유의 | ✅ 유의 | ✅ 내 | **ETF 날짜가 아닌 다른 날 변화** | ★★★☆☆ |
| ❌ 비유의 | ❌ 비유의 | ✅ 내 | **구조변화 없음** | ★★★★☆ |

### 7.3 변화점 날짜 비교

```python
def compare_breakpoints(chow_date, qa_date, tolerance_days=30):
    """
    Chow와 QA의 변화점 날짜 비교

    Parameters:
    -----------
    chow_date : datetime (지정한 날짜)
    qa_date : datetime (데이터에서 찾은 날짜)
    tolerance_days : int (허용 오차, 일)

    Returns:
    --------
    str: 해석
    """

    diff_days = abs((qa_date - chow_date).days)

    if diff_days == 0:
        return "완벽히 일치 (지정 날짜 = 발견 날짜)"
    elif diff_days <= tolerance_days:
        return f"근접 일치 ({diff_days}일 차이, 허용 범위 내)"
    else:
        return f"불일치 ({diff_days}일 차이, 다른 이벤트 가능성)"
```

---

## 8. 표준 보고 양식

### 8.1 요약 통계

```python
# 표준 요약 보고서
summary_report = f"""
================================================================================
구조변화 검정 표준 보고서
================================================================================

【데이터 정보】
  - 기간: {df.index[0]} ~ {df.index[-1]}
  - 샘플 수: {len(df)}일
  - 변수 수: {len(variables)}개
  - 변화점 날짜: {BREAKPOINT_DATE} (ETF 승인일)

【Chow Test 결과】
  - 검정 변수 수: {len(variables)}
  - Bonferroni 유의: {bonf_significant}개 ({bonf_significant/len(variables)*100:.1f}%)
  - FDR 유의: {fdr_significant}개 ({fdr_significant/len(variables)*100:.1f}%)

  상위 5개 변수:
{chow_top5_table}

【Quandt-Andrews Test 결과】
  - 검정 변수 수: {len(variables)}
  - 5% 유의: {qa_significant_5pct}개
  - 1% 유의: {qa_significant_1pct}개

  상위 5개 변수:
{qa_top5_table}

【CUSUM Test 결과】
  - 경계 이탈 변수: {cusum_breaches}개
  - 점진적 변화: {cusum_gradual}개

  변화 속도 분포:
    - 급격: {cusum_abrupt}개
    - 빠름: {cusum_rapid}개
    - 중간: {cusum_moderate}개
    - 느림: {cusum_slow}개

【종합 결론】
{overall_conclusion}

================================================================================
"""

# 저장
with open('results/structural_change_summary.txt', 'w', encoding='utf-8') as f:
    f.write(summary_report)
```

### 8.2 시각화 표준

```python
import matplotlib.pyplot as plt
import seaborn as sns

# 한글 폰트 설정
plt.rcParams['font.family'] = 'AppleGothic'
plt.rcParams['axes.unicode_minus'] = False

def plot_standard_results(chow_results, qa_results, cusum_results):
    """표준 시각화"""

    fig, axes = plt.subplots(2, 2, figsize=(16, 12))

    # 1. Chow Test: F-통계량 상위 30개
    top30_chow = chow_results.nlargest(30, 'F_stat')
    axes[0, 0].barh(top30_chow['Variable'], top30_chow['F_stat'])
    axes[0, 0].set_xlabel('F-통계량')
    axes[0, 0].set_title('Chow Test: F-통계량 TOP 30')
    axes[0, 0].axvline(x=10, color='r', linestyle='--', label='일반적 유의 기준')

    # 2. QA Test: sup F-통계량 상위 30개
    top30_qa = qa_results.nlargest(30, 'sup_F')
    axes[0, 1].barh(top30_qa['Variable'], top30_qa['sup_F'])
    axes[0, 1].set_xlabel('sup F-통계량')
    axes[0, 1].set_title('Quandt-Andrews: sup F TOP 30')
    axes[0, 1].axvline(x=9.21, color='r', linestyle='--', label='5% CV')

    # 3. 변화점 날짜 분포
    qa_dates = pd.to_datetime(qa_results['Breakpoint_Date'])
    axes[1, 0].hist(qa_dates, bins=50, edgecolor='black')
    axes[1, 0].axvline(x=pd.to_datetime(BREAKPOINT_DATE),
                       color='r', linestyle='--', linewidth=2,
                       label='ETF 승인일')
    axes[1, 0].set_xlabel('변화점 날짜')
    axes[1, 0].set_ylabel('변수 개수')
    axes[1, 0].set_title('Quandt-Andrews: 변화점 날짜 분포')
    axes[1, 0].legend()

    # 4. CUSUM: 변화 유형 분포
    change_type_counts = cusum_results['Change_Type'].value_counts()
    axes[1, 1].pie(change_type_counts, labels=change_type_counts.index,
                   autopct='%1.1f%%')
    axes[1, 1].set_title('CUSUM: 변화 유형 분포')

    plt.tight_layout()
    plt.savefig('results/structural_change_tests_summary.png', dpi=300, bbox_inches='tight')
    plt.close()
```

---

## 9. 체크리스트

### 9.1 실행 전 체크리스트

- [ ] **데이터 확인**
  - [ ] 결측치 0개 확인
  - [ ] 이상치 처리 완료 (Winsorization)
  - [ ] 변수 표준화 완료 (Z-score)
  - [ ] 시계열 순서 확인 (오름차순 정렬)

- [ ] **파라미터 설정**
  - [ ] 변화점 날짜 지정 (BREAKPOINT_DATE)
  - [ ] QA 탐색 구간 설정 (trim=0.15)
  - [ ] 다중 검정 보정 방법 선택 (Bonferroni/FDR)
  - [ ] HAC lag 설정 (maxlags=5 권장)

- [ ] **환경 설정**
  - [ ] Python 패키지 설치 (statsmodels, scipy, pandas)
  - [ ] 결과 저장 폴더 생성 (results/)
  - [ ] 한글 폰트 설정 (시각화용)

### 9.2 실행 중 체크리스트

- [ ] **Chow Test**
  - [ ] 모든 변수에 대해 검정 완료
  - [ ] F-통계량, p-value 계산
  - [ ] Bonferroni, FDR 보정 적용
  - [ ] 계수 변화율 계산

- [ ] **Quandt-Andrews Test**
  - [ ] 탐색 구간 내 모든 변화점 검정
  - [ ] sup F-통계량 계산
  - [ ] 변화점 날짜 기록
  - [ ] Critical value 비교

- [ ] **CUSUM Test**
  - [ ] CUSUM 경로 계산
  - [ ] 경계선 계산
  - [ ] 경계 이탈 여부 확인
  - [ ] 변화 속도 분류

### 9.3 실행 후 체크리스트

- [ ] **결과 검증**
  - [ ] 3가지 검정 결과 일관성 확인
  - [ ] 변화점 날짜 비교 (Chow vs QA)
  - [ ] 이상 결과 재확인 (F-stat > 10,000 등)

- [ ] **결과 저장**
  - [ ] CSV 파일 저장 (chow, qa, cusum)
  - [ ] 요약 보고서 저장 (TXT)
  - [ ] 시각화 저장 (PNG)

- [ ] **문서화**
  - [ ] 검정 조건 기록
  - [ ] 주요 발견사항 정리
  - [ ] 한계점 및 주의사항 기록

---

## 10. Python 구현 예시 (전체 파이프라인)

```python
import pandas as pd
import numpy as np
import statsmodels.api as sm
from scipy import stats
from statsmodels.stats.multitest import multipletests
import matplotlib.pyplot as plt

class StructuralChangeTestingFramework:
    """구조변화 검정 표준 프레임워크"""

    def __init__(self, df, y_col, breakpoint_date, trim=0.15):
        """
        Parameters:
        -----------
        df : pd.DataFrame (표준화된 데이터)
        y_col : str (종속변수 컬럼명)
        breakpoint_date : str (변화점 날짜, 'YYYY-MM-DD')
        trim : float (QA 탐색 구간 제외 비율)
        """
        self.df = df
        self.y_col = y_col
        self.breakpoint_date = pd.to_datetime(breakpoint_date)
        self.trim = trim

        # 변화점 인덱스
        self.breakpoint_idx = self.df.index.get_loc(self.breakpoint_date)

        # 결과 저장
        self.chow_results = None
        self.qa_results = None
        self.cusum_results = None

    def run_all_tests(self, variables):
        """전체 검정 실행"""

        print("=" * 80)
        print("구조변화 검정 시작")
        print("=" * 80)

        # Step 1: Chow Test
        print("\n[1/3] Chow Test 실행 중...")
        self.chow_results = self._run_chow_test(variables)
        print(f"완료: {len(self.chow_results)}개 변수 검정")

        # Step 2: Quandt-Andrews Test
        print("\n[2/3] Quandt-Andrews Test 실행 중...")
        self.qa_results = self._run_qa_test(variables)
        print(f"완료: {len(self.qa_results)}개 변수 검정")

        # Step 3: CUSUM Test
        print("\n[3/3] CUSUM Test 실행 중...")
        self.cusum_results = self._run_cusum_test(variables)
        print(f"완료: {len(self.cusum_results)}개 변수 검정")

        print("\n" + "=" * 80)
        print("모든 검정 완료!")
        print("=" * 80)

        return self.chow_results, self.qa_results, self.cusum_results

    def _run_chow_test(self, variables):
        """Chow Test 실행"""
        results = []

        for var in variables:
            y = self.df[self.y_col]
            X = sm.add_constant(self.df[[var]])

            result = self._chow_test_single(y, X, self.breakpoint_idx)
            result['Variable'] = var
            results.append(result)

        df_results = pd.DataFrame(results)

        # 다중 검정 보정
        reject_bonf, pvals_bonf, _, _ = multipletests(
            df_results['p_value'], alpha=0.05, method='bonferroni'
        )
        reject_fdr, pvals_fdr, _, _ = multipletests(
            df_results['p_value'], alpha=0.05, method='fdr_bh'
        )

        df_results['Significant_Bonferroni'] = reject_bonf
        df_results['Significant_FDR'] = reject_fdr
        df_results['p_value_Bonferroni'] = pvals_bonf
        df_results['p_value_FDR'] = pvals_fdr

        return df_results.sort_values('F_stat', ascending=False)

    def _chow_test_single(self, y, X, breakpoint_idx):
        """단일 변수 Chow Test"""
        # (이전 코드 참조)
        pass

    def _run_qa_test(self, variables):
        """Quandt-Andrews Test 실행"""
        # (이전 코드 참조)
        pass

    def _run_cusum_test(self, variables):
        """CUSUM Test 실행"""
        # (이전 코드 참조)
        pass

    def save_results(self, output_dir='results'):
        """결과 저장"""
        import os
        os.makedirs(output_dir, exist_ok=True)

        self.chow_results.to_csv(f'{output_dir}/chow_test_results.csv', index=False)
        self.qa_results.to_csv(f'{output_dir}/quandt_andrews_results.csv', index=False)
        self.cusum_results.to_csv(f'{output_dir}/cusum_test_results.csv', index=False)

        print(f"\n결과 저장 완료: {output_dir}/")

    def plot_results(self):
        """표준 시각화"""
        # (이전 코드 참조)
        pass

# 사용 예시
if __name__ == "__main__":
    # 1. 데이터 로드 및 표준화
    df = pd.read_csv('integrated_data_full_v2.csv', index_col='Date', parse_dates=True)
    df_scaled = standardize_data(df)  # 표준화 함수

    # 2. 프레임워크 초기화
    framework = StructuralChangeTestingFramework(
        df=df_scaled,
        y_col='Close',
        breakpoint_date='2024-01-10',
        trim=0.15
    )

    # 3. 검정 실행
    variables = [col for col in df_scaled.columns if col != 'Close']
    chow, qa, cusum = framework.run_all_tests(variables)

    # 4. 결과 저장
    framework.save_results()

    # 5. 시각화
    framework.plot_results()
```

---

## 11. 한계점 및 주의사항

### 11.1 표준화의 한계

⚠️ **표준화 후에도 주의할 점**:
- 비선형 관계는 포착하지 못함
- 시차 효과 (lagged effect) 미반영
- 다변량 상호작용 무시 (단변량 검정이므로)

### 11.2 다중 검정의 딜레마

⚠️ **Bonferroni vs FDR**:
- Bonferroni: 너무 보수적 (Type II 오류 증가)
- FDR: 거짓 발견 가능성 (Type I 오류)
- **권장**: 둘 다 보고, FDR 우선 해석

### 11.3 변화점 날짜의 불확실성

⚠️ **QA Test 변화점 해석**:
- 데이터 기반 변화점 ≠ 진짜 인과 날짜
- ETF 승인일과 다를 수 있음 (정상)
- 여러 변수의 변화점 분포를 봐야 함

### 11.4 CUSUM의 민감도

⚠️ **경계 이탈 해석**:
- 경계 이탈 = 급격한 변화이지만
- 경계 내 ≠ 변화 없음!
- 점진적이지만 큰 변화도 가능

---

## 12. 참고문헌

1. **Chow, G. C. (1960)**. "Tests of Equality Between Sets of Coefficients in Two Linear Regressions". *Econometrica*, 28(3), 591-605.

2. **Andrews, D. W. K. (1993)**. "Tests for Parameter Instability and Structural Change with Unknown Change Point". *Econometrica*, 61(4), 821-856.

3. **Brown, R. L., Durbin, J., & Evans, J. M. (1975)**. "Techniques for Testing the Constancy of Regression Relationships over Time". *Journal of the Royal Statistical Society: Series B*, 37(2), 149-163.

4. **Newey, W. K., & West, K. D. (1987)**. "A Simple, Positive Semi-Definite, Heteroskedasticity and Autocorrelation Consistent Covariance Matrix". *Econometrica*, 55(3), 703-708.

5. **Benjamini, Y., & Hochberg, Y. (1995)**. "Controlling the False Discovery Rate: A Practical and Powerful Approach to Multiple Testing". *Journal of the Royal Statistical Society: Series B*, 57(1), 289-300.

---

**작성일**: 2025-11-09
**버전**: 1.0
**라이선스**: MIT

© 2025 Bitcoin ETF Structural Change Analysis Project
